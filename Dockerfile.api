# NanoClaw API Dockerfile
FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm install --production

# 构建
COPY . .
RUN npm run build

# 暴露端口
EXPOSE 3456

# 启动
CMD ["node", "dist/index.js"]
